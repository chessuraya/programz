#include <stdio.h>
#include <math.h>
int func(){
    float a=0;
    float b=0;
    int i=0;

    scanf ("%f", &a);
    a = sqrt(a);
    if (feof(stdin))
    if (feof(stdin))
    return 0;
    func();
    printf("%.4f\n",a);
    return 0;

}


main()
{
func();
}
